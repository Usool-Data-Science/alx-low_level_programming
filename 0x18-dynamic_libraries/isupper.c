#include "main.h"
/**
  * _isupper - check if c is upper case
  * @c: the variable to check
  *
  * Return: 0 for successful exit
  */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
